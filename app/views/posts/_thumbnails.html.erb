<% @posts.each do |post| %>
  <div class="widget">
    <div class="thumbnail">
      <%= link_to image_tag(post.photo.url(:thumb), class: "thumb"), post_path(post.id) %>
    </div>
  </div>
<% end %>
